// Expresiones regulares que se usan en la validacion de dtos

export class RegexValidators {
    public static readonly NAME: RegExp = /./;
    public static readonly PASSWORD: RegExp = /./;
    public static readonly PHONE: RegExp = /./;
}
